Scraper code for downloading official DERC PDFs

All scripts are path-agnostic.
No hardcoded directories.
Run using CLI args only.

Example:
python derc_scrape_pdfs.py --start-url <url> --scope-path <path> --out data/raw
